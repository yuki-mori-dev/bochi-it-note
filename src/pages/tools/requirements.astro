---
import BaseLayout from "../../layouts/BaseLayout.astro";
const title = '要件で探す（統合先: 探す）｜Bochi IT Note';
const description = '本ページは /search に統合されました。';
---

<BaseLayout {title} {description} noindex={true}>
	<h1 class="text-2xl font-bold">要件で探す → 探す に統合</h1>
	<div class="mt-4 rounded-md border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">
		<p>本ページは <a class="underline" href="/search">「探す」</a> に統合されました。数秒後に自動で遷移します。</p>
	</div>
	<script is:inline>
		setTimeout(()=>{ location.replace('/search'); }, 800);
	</script>
</BaseLayout>


