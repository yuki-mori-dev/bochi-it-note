---
const provider = import.meta.env.PUBLIC_NEWSLETTER_EMBED || '';
---

{provider ? (
	<section class="rounded-md border border-gray-200 bg-white p-4">
		<h2 class="text-sm font-semibold text-gray-900">更新を受け取る</h2>
		<p class="mt-1 text-xs text-gray-600">比較・要件更新・テンプレ追加をお知らせします。</p>
		<div class="mt-3" set:html={provider}></div>
	</section>
) : null}


